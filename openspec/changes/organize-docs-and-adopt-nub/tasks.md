# Tasks

- [x] Move the Astro documentation application into `apps/docs` and preserve all routes and presentation.
- [x] Adopt Nub workspace identity and lockfile, use mise to provision pinned `just` and Nub versions locally and in GitHub Actions, retain the root `justfile` as the only task runner, and retain npm only for Trusted Publishing.
- [x] Make documentation projections ephemeral build outputs while retaining tracked plugin manifest checks.
- [x] Add scoped Biome formatting and linting for authored docs code and repository tooling.
- [x] Update contributor documentation and active project references to the root `justfile` helpers backed by Nub.
- [x] Verify cold and repeat documentation builds, routes, package smoke tests, full repository checks and visual behavior.
- [x] Review `release.md` against what was actually delivered.
