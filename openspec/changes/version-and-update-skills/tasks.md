# Tasks

- [ ] Add independent `version` and `role` metadata to every public skill.
- [ ] Generate deterministic per-package manifests and skill histories.
- [ ] Add immutable per-skill snapshot and receipt storage without rewriting legacy cache.
- [ ] Add `skills status` with human and JSON output, including optional remote comparison.
- [ ] Add exact update planning and drift-checked `skills apply --plan` for one skill.
- [ ] Add an official GitHub `master` source and preserve explicit local catalogs.
- [ ] Test same-version revisions, one-skill isolation, drift, interruption, offline failure, malformed packages and platform links.
- [ ] Update generated documentation and CLI contracts.
- [ ] Run repository and system verification, review the release story and archive the change.
