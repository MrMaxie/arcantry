# Tasks

- [ ] Define one canonical identity source and explicit host projection allowlists.
- [ ] Align the existing Codex and Claude plugin manifests without adding unsupported cross-host fields.
- [ ] Validate canonical checkout and packaged plugin projections for identity and host contract drift.
- [ ] Run focused catalog, package identity and plugin manifest checks.
- [ ] Run `just check` and strict OpenSpec validation.
- [ ] Review `release.md` against what was actually delivered.
