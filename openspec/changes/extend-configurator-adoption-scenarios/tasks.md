# Tasks

- [ ] Define a bounded scenario catalog and stable scenario ids against the existing question model.
- [ ] Add scenario selection without preselecting user-specific scope or authority.
- [ ] Add focused follow-up gates for conflicting or materially uncertain answers.
- [ ] Generate distinct read-only evaluation, initial adoption and existing-setup requests from one normalized model.
- [ ] Preserve scenario and answer state through the existing URL contract.
- [ ] Verify keyboard, screen-reader, reload, back-navigation and reduced-motion behavior for every scenario path.
- [ ] Run focused configurator tests, documentation checks, `just check` and strict OpenSpec validation.
- [ ] Review `release.md` against what was actually delivered.
