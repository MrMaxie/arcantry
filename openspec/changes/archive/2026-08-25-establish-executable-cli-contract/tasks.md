# Tasks

- [x] Add the complete tracked command inventory and independent Rust contract suite.
- [x] Cover every leaf command, invalid arguments, preview/apply boundaries and all repository transition strategies.
- [x] Replace the TypeScript differential oracle and text-presence scenario check.
- [x] Run `just native-conformance`, Rust tests and strict OpenSpec validation.
- [x] Review `release.md` against what was actually delivered.
- [x] Make project-plan staging and commit failures restore the exact pre-apply filesystem tree.
- [x] Route shared and private `repo init`, `repo update` and `repo remove` through transactional project plans.
- [x] Make multi-target skill link and unlink operations, including private Git exclusion, rollback as one operation.
- [x] Add deterministic failure-position and property tests for write, delete, delete-tree and link transactions.
- [x] Bind every command and trust claim to an executable scenario-registry dispatcher.
- [x] Require the current canonical project root and exact authority for every external plan path.
- [x] Cover reversible finalization and report post-commit cleanup failures as warnings.
- [x] Re-run native conformance, the full host gate, the disposable Linux system gate and strict OpenSpec validation.
