# Tasks

- [x] Add independent `version` and `role` metadata to every public skill.
- [x] Generate deterministic per-package manifests and skill histories.
- [x] Add immutable per-skill snapshot and receipt storage without rewriting legacy cache.
- [x] Add `skills status` with human and JSON output, including optional remote comparison.
- [x] Add exact update planning and drift-checked `skills apply --plan` for one skill.
- [x] Add an official GitHub `master` source and preserve explicit local catalogs.
- [x] Test same-version revisions, one-skill isolation, drift, interruption, offline failure, malformed packages and platform links.
- [x] Update generated documentation and CLI contracts.
- [x] Run repository and system verification, review the release story and archive the change.
