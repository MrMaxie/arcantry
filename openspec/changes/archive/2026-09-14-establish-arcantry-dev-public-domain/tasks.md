# Tasks

- [x] Inventory every tracked public URL, root-relative documentation route, generated metadata owner and deployed asset class before implementation.
- [x] Configure the documentation for `https://arcantry.dev/`, root deployment and content-hashed `/static/` assets.
- [x] Update authored public links, schema identities, runtime defaults and generated plugin metadata to the canonical origin.
- [x] Add generated-output verification for canonical metadata, sitemap URLs, root-relative links and the asset directory.
- [x] Repeat the public URL and route inventory and mark every candidate compliant or explicitly out of scope.
- [x] Run focused tests, the documentation build, generated-output verification, `just check` and strict OpenSpec validation.
- [x] Verify `arcantry.dev` for the GitHub account and configure it as this repository's GitHub Pages custom domain.
- [x] Verify apex and `www` DNS reachability, approved custom-domain certificate and enforced HTTPS.
- [x] Verify Cloudflare proxying, HTTP-to-HTTPS redirect, apex canonical redirect and the six-month `/static/` cache rule.
- [x] Confirm Cloudflare `Full (strict)` origin TLS; keep HSTS disabled by product decision.
- [x] Verify the repository homepage, apex, `www`, canonical metadata, sitemaps, schema URLs, redirects and warm-cache headers live.
- [x] Review `release.md` against the delivered outcome with HSTS excluded.
