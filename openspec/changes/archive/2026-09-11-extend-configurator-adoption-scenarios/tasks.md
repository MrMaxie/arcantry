# Tasks

- [x] Define a bounded scenario catalog and stable scenario ids against the existing question model.
- [x] Add scenario selection without preselecting user-specific scope or authority.
- [x] Keep scenarios as hints and ask one focused question when an answer creates a material conflict.
- [x] Generate distinct read-only evaluation, initial adoption and existing-setup requests from one normalized model.
- [x] Preserve scenario and answer state through the existing URL contract.
- [x] Verify semantic controls, accessible progress, URL reload state and motion-free reflow across the scenario model.
- [x] Run focused configurator tests, documentation checks, live browser verification, `just check-host` and strict OpenSpec validation.
- [x] Review `release.md` against what was actually delivered.
