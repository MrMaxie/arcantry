# Approach

Keep the four command choices inside their ARIA tablist. Place the download link in a separate action wrapper that consumes the remaining horizontal space, adds a divider before the action, and uses the existing restrained border and surface tokens for a button-like treatment.

On narrow layouts, keep the command tabs together and place the download action on a separate row aligned to the right. This preserves reading and keyboard order without squeezing the command choices.
