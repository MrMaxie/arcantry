# Why

Repositories need a small, reusable lifecycle contract where product intent survives implementation and can drive versioning and release history directly.

# What changes

Establish Arcantry with OpenSpec-derived releases, a stable `just` command surface, `mise` tool pinning, tested release tooling, Astro/Starlight documentation and GitHub Actions automation.
