# Tasks

- [x] Normalize legacy specification headings and add scenarios without changing their contracts.
- [x] Validate every current specification and active change in strict non-interactive mode.
- [x] Gate Pages artifact creation on `just ci`.
- [x] Run `just ci` and verify the release artifact against the delivered behavior.
