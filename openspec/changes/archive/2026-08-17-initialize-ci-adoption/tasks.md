# Tasks

- [x] Add an explicit, idempotent CI adoption recipe using the built public CLI.
- [x] Run the adoption recipe before public repository and skill validation in `just ci`.
- [x] Document the clean-checkout self-validation sequence.
- [x] Verify clean repository initialization through the CLI test and `just ci` command order.
