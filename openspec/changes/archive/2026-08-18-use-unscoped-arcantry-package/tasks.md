# Tasks

- [x] Rename the combined package to `arcantry`, distinguish the private workspace root and regenerate package-manager state.
- [x] Update public launcher examples, release validation and package identity tests.
- [x] Verify the exact packed archive through CLI and subpath import smoke tests.
- [x] Run strict OpenSpec validation, `just check` and the repository build.
  - Strict OpenSpec validation, types, 103 tests, catalog validation, generated-state checks and the build pass. `just check` reaches the expected active-change release boundary.
- [x] Review `release.md` against what was actually delivered.
