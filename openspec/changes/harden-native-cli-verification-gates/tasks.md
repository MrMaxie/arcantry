# Tasks

- [ ] Remove the retired TypeScript-oracle requirement and align the contributor description of `just native-conformance`.
- [ ] Version the CLI contract inventory with required behavior dimensions for every leaf command.
- [ ] Add independent native evidence for every missing preview, apply, rejection and rollback dimension.
- [x] Make the pinned coverage run emit line and branch records for every production Rust file.
- [x] Record reviewed per-file floors, cover every zero-evidence module and add the coverage gate to CI.
- [ ] Keep host, Testcontainers Linux and release-target execution as separate reported gates.
- [x] Run `just check`, `just native-conformance`, `just rust-coverage`, `just linux-system-test`, package smoke and strict OpenSpec validation.
- [ ] Review `release.md` against what was actually delivered.
