---
const hero = Astro.locals.starlightRoute.entry.data.hero;
if (!hero) throw new Error('ArcantryHero requires hero frontmatter');
---

<section class="hero-shell">
  <div class="hero-copy">
    <h1>{hero.title}</h1>
    {hero.tagline && <p class="hero-tagline">{hero.tagline}</p>}
    {hero.actions?.length && (
      <div class="hero-actions">
        {hero.actions.map((action) => (
          <a class:list={['hero-action', action.variant === 'primary' ? 'primary' : 'secondary']} href={action.link}>
            {action.text}
          </a>
        ))}
      </div>
    )}
  </div>
</section>

<style>
  @layer starlight.core {
    .hero-shell {
      min-height: 24rem;
      display: flex;
      align-items: center;
      padding: 4.8rem 0 4.2rem;
    }
    .hero-copy { max-width: 52rem; }
    h1 {
      margin: 0;
      max-width: 12ch;
      font-family: var(--ar-font-display);
      font-size: clamp(3.5rem, 6.2vw, 6.5rem);
      font-weight: 400;
      line-height: .92;
      letter-spacing: -.045em;
      color: #232426;
    }
    .hero-tagline {
      max-width: 39rem;
      margin: 1.7rem 0 0;
      color: #5d5e60;
      font-size: 1.02rem;
      line-height: 1.65;
      letter-spacing: -.015em;
    }
    .hero-actions { display: flex; flex-wrap: wrap; gap: .7rem; margin-top: 2rem; }
    .hero-action {
      padding: .72rem 1rem;
      border-radius: .35rem;
      border: 1px solid #232426;
      font-size: .8rem;
      font-weight: 650;
      text-decoration: none;
    }
    .hero-action.primary { color: #faf9f8; background: #232426; }
    .hero-action.secondary { color: #232426; background: transparent; border-color: #d0cfcf; }
    .hero-action.secondary:hover { border-color: #232426; }

    @media (max-width: 49.99rem) {
      .hero-shell { min-height: auto; padding: 3.5rem 0 3rem; }
      h1 { font-size: clamp(3.2rem, 17vw, 5rem); }
    }
  }
</style>
