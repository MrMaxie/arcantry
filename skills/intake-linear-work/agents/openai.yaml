interface:
  display_name: "Intake Linear Work"
  short_description: "Capture repository work in Linear"
  default_prompt: "Use $intake-linear-work to capture this task in Linear."

dependencies:
  tools:
    - type: "mcp"
      value: "linear"
      description: "Linear connector for profile resolution, projects, issues, documents, and comments."

policy:
  allow_implicit_invocation: true

